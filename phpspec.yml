suites:
    main:
        spec_path: tests
        namespace: Cerbero\Workflow
        psr4_prefix: Cerbero\Workflow
extensions:
    - PhpSpec\Extension\CodeCoverageExtension
code_coverage:
    format:
      - clover
    output:
      clover: coverage.clover
